import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  paperChip: {
    height: 200,
    marginTop: 10,
  },
  chip: {
    marginTop: 10,
  },
}));
